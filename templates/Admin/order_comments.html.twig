{% set order = hookable_metadata.context.resource %}

{{ render(controller(
    'ThreeBRS\\OrderCommentsPlugin\\Controller\\OrderMessageController::save', {orderId: order.id}
)) }}

{{ render(controller(
    'ThreeBRS\\OrderCommentsPlugin\\Controller\\OrderMessageController::show', {orderId: order.id}
)) }}
