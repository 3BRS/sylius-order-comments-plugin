parameters:
    env(DATABASE_URL): ''

doctrine:
    dbal:
        driver: 'pdo_mysql'
        server_version: '8.0.32'
        charset: UTF8
        url: '%env(resolve:DATABASE_URL)%'
    orm:
        enable_lazy_ghost_objects: true
