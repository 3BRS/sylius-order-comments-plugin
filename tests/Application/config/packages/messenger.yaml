framework:
    messenger:
        default_bus: sylius_default.bus
        buses:
            sylius_default.bus: ~
            sylius.command_bus:
                middleware:
                    - validation
                    - doctrine_transaction
            sylius.event_bus: ~
            sylius_event.bus: ~
